% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict_rsymbolic2.R
\name{fitted.rsymbolic2}
\alias{fitted.rsymbolic2}
\title{Fitted values from a symbolic regression fit}
\usage{
\method{fitted}{rsymbolic2}(object, expression = NULL, ...)
}
\arguments{
\item{object}{An object of class \code{"rsymbolic2"} returned by
\code{\link{symbolic_regression}}.}

\item{expression}{Which fitted expression to evaluate. \code{NULL} (default)
uses \code{object$recommended}; otherwise an expression string, e.g.
\code{object$expression} for the lowest-loss model or any row of
\code{object$pareto_front$expression}.}

\item{...}{Ignored (present for S3 compatibility).}
}
\value{
Numeric vector of fitted values, one per training observation.
}
\description{
Evaluate a fitted expression on the training data stored on the object, which
requires the fit to have been made with \code{keep_data = TRUE} (the default).
}
\examples{
\donttest{
X <- matrix(seq(-3, 3, length.out = 20), ncol = 1)
y <- 2 * X[, 1] + 1
res <- symbolic_regression(X, y, population_size = 200L, generations = 40L,
                           seed = 1L)
head(fitted(res))
head(residuals(res))
}

}
\seealso{
\code{\link{residuals.rsymbolic2}} for the same values as deviations
  from \code{y}, and \code{\link{predict.rsymbolic2}} to evaluate the fit on
  new data.
}
